[package]
name = "ignix-core"
version.workspace = true
edition.workspace = true
description = "Boot loader core made purely in Rust to ensure stability and speed."
license.workspace = true

[[bin]]
name = "ignixx64"
path = "src/main.rs"

[dependencies]
ignix-sdk = { path = "../ignix-sdk/" }
