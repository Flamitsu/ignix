[package]
name = "ignix-sdk"
version.workspace = true
edition.workspace = true
description = "Safe UEFI runtime and SDK designed for modular bootloader development in Rust."
license.workspace = true

[dependencies]
